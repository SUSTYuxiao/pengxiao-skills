[project]
name = "pengxiao-skills"
version = "0.1.0"
description = "个人 Agent Skills 分发 CLI（light 拷贝 + 软链激活）"
requires-python = ">=3.8"
dependencies = []

[project.scripts]
pskills = "pskills.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pskills"]
